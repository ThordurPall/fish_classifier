learning_rate: {min: 0.0001, max: 0.1}
batch_size: {min: 50, max: 500, discretization_step: 50}
activation: ["relu", "leaky_relu"]
dropout_p: {min: 0.0, max: 0.8}
seed: 0
epochs: 20
use_azure: True
n_trials: 50
n_warmup_steps: 5
n_startup_trials: 10